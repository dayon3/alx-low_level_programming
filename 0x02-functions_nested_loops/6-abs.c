#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *
 * @n: input number as an integer
 *
 * Return: absolute value
 */

int _abs(int n)
{
	return (abs(n));
}
